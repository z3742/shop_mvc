# 2026-07-14 工作日志

## 修复 shop_db.sql 导入错误

- **问题**：phpMyAdmin 导入 `database/shop_db.sql` 时报错 `#1064`，原因是 MySQL 5.7 不识别 `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` 语法。
- **修复**：将该语句改写为存储过程判断字段是否存在，再动态执行 `ALTER TABLE`，兼容 MySQL 5.7 / 8.0。
- **修改文件**：`database/shop_db.sql` 第 383 行附近的 `sales` 字段新增逻辑。
